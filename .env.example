# Gemini API (optional — rule-based fallback works without it)
# GOOGLE_API_KEY=your_gemini_api_key_here

# Elastic Cloud (required for threat intel + cross-scan correlation)
ES_CLOUD_ID=your_elastic_cloud_id
ES_API_KEY=your_elastic_api_key
